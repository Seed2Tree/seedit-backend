<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seedit.feature.study.repository.StudyRepository">

    <resultMap id="studyResultMap" type="com.seedit.feature.study.domain.InvestmentStudy">
        <id property="isid" column="isid"/>
        <result property="youtubeUrl" column="youtube_url"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="studyResultMap">
        SELECT isid, title, youtube_url, description, category, thumbnail, created_at
        FROM investment_study
        ORDER BY isid ASC
    </select>

    <select id="findByCategory" resultMap="studyResultMap">
        SELECT isid, title, youtube_url, description, category, thumbnail, created_at
        FROM investment_study
        WHERE category = #{category}
        ORDER BY isid ASC
    </select>

    <select id="findById" resultMap="studyResultMap">
        SELECT isid, title, youtube_url, description, category, thumbnail, created_at
        FROM investment_study
        WHERE isid = #{isid}
    </select>

    <!-- 즐겨찾기 추가 -->
    <insert id="insertBookmark">
        INSERT INTO study_bookmark (user_id, isid) VALUES (#{userId}, #{isid})
    </insert>

    <!-- 즐겨찾기 삭제 -->
    <delete id="deleteBookmark">
        DELETE FROM study_bookmark WHERE user_id = #{userId} AND isid = #{isid}
    </delete>


    <delete id="deleteByUserId">
        DELETE FROM study_bookmark WHERE user_id = #{userId}
    </delete>

    <!-- 중복 체크 -->
    <select id="countBookmark" resultType="int">
        SELECT COUNT(*) FROM study_bookmark WHERE user_id = #{userId} AND isid = #{isid}
    </select>

    <!-- 즐겨 찾기 개수 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM study_bookmark WHERE user_id = #{userId}
    </select>

    <!-- 내 즐겨찾기 isid 목록 (프론트 상태 초기화용) -->
    <select id="findBookmarkIdsByUserId" resultType="Long">
        SELECT isid FROM study_bookmark WHERE user_id = #{userId}
    </select>

    <!-- 내 즐겨찾기 콘텐츠 목록 (마이페이지용) -->
    <select id="findBookmarkedByUserId" resultMap="studyResultMap">
        SELECT s.isid, s.title, s.youtube_url, s.description, s.category, s.thumbnail, s.created_at
        FROM investment_study s
        JOIN study_bookmark b ON s.isid = b.isid
        WHERE b.user_id = #{userId}
        ORDER BY b.created_at DESC
    </select>

</mapper>
